# =============================================================================
# Aletheia — Environment Variables Template
# Copy file này thành .env và điền giá trị thật
# cp .env.example .env
# =============================================================================

# ── EAS / Expo ───────────────────────────────────────────────────────────────
# Lấy từ: npx eas project:init
EXPO_PUBLIC_EAS_PROJECT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# Username trên expo.dev
EXPO_PUBLIC_OWNER_NAME=your-expo-username

# ── Shared Auth Contract (server + client) ────────────────────────────────────
# Các giá trị này là canonical cho server. Client dùng các biến EXPO_PUBLIC_ mirror bên dưới.
APP_ID=
OAUTH_PORTAL_URL=
OAUTH_SERVER_URL=
OWNER_OPEN_ID=
OWNER_NAME=

# ── Server-only auth/runtime ───────────────────────────────────────────────────
# BẮT BUỘC cho server: dùng để ký session JWT
JWT_SECRET=replace-with-a-long-random-secret
# Optional: override API server port (default 3000)
# PORT=3000

# ── Expo / client mirrors ──────────────────────────────────────────────────────
# Các biến này nên mirror cùng giá trị với block Shared Auth Contract.
EXPO_PUBLIC_OAUTH_PORTAL_URL=
EXPO_PUBLIC_OAUTH_SERVER_URL=
EXPO_PUBLIC_APP_ID=
EXPO_PUBLIC_OWNER_OPEN_ID=
EXPO_PUBLIC_API_BASE_URL=

# ── AI Provider Keys (SERVER-SIDE ONLY) ──────────────────────────────────────
# QUAN TRỌNG: Không dùng prefix EXPO_PUBLIC_ cho các keys này.
# Keys này chỉ được đọc bởi Express server (server/_core/index.ts).
# Không bao giờ expose ra client bundle.
# Xem: docs/BLUEPRINT.md → Security section
ALETHEIA_CLAUDE_API_KEY=sk-ant-api03-...
ALETHEIA_OPENAI_API_KEY=sk-proj-...
ALETHEIA_GEMINI_API_KEY=AIza...

# Temporary hardening only: this secret is sent by the app in a request header.
# It reduces anonymous scraping of aiConfig but does NOT replace real server-side key isolation.
EXPO_PUBLIC_ALETHEIA_APP_SECRET=your-32-char-hex-secret-here
ALETHEIA_APP_SECRET=your-32-char-hex-secret-here

# ── Gift Backend (Cloudflare Workers) ────────────────────────────────────────
# URL của CF Worker đã deploy (xem gift-backend/)
EXPO_PUBLIC_GIFT_BACKEND_URL=https://your-worker.your-subdomain.workers.dev

# ── Optional server inference/model backends ──────────────────────────────────
# Chỉ cần nếu bạn dùng các route ảnh / LLM proxy phía server.
# BUILT_IN_FORGE_API_URL=
# BUILT_IN_FORGE_API_KEY=
# AI_API_URL=
# AI_API_KEY=
# AI_MODEL=

# ── RevenueCat ───────────────────────────────────────────────────────────────
# Lấy từ app.revenuecat.com → Project → API Keys
EXPO_PUBLIC_REVENUECAT_ANDROID_API_KEY=goog_...
EXPO_PUBLIC_REVENUECAT_IOS_API_KEY=appl_...

# ── Development overrides ────────────────────────────────────────────────────
# Port Metro bundler (default: 8081)
# EXPO_PORT=8081

# =============================================================================
# KHÔNG commit file .env vào git.
# .env đã có trong .gitignore.
# =============================================================================
